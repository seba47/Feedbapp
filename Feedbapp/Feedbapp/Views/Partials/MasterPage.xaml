<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Feedbapp.Views.MasterPage"
             Padding="0,40,0,0"
             Icon="greenHamb.png"
             Title="-">
  <ContentPage.Content>
    <ListView x:Name="listView" VerticalOptions="FillAndExpand">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <StackLayout HorizontalOptions="Center" Padding="0,5,0,5">
              <Label Text="{Binding Name}" FontSize="22" TextColor="White" HorizontalOptions="Center" />
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </ContentPage.Content>
</ContentPage>